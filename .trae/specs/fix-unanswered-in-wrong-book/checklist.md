# Checklist

* [x] `calcConsecutive` 中 `isCorrect === null` 时返回上次有效 `consecutiveCorrect`，不归零

* [x] `getWrong` 中遍历 `details` 时跳过 `isCorrect === null` 的记录

* [x] `recordController.js` 总行数 ≤ 150 行

* [x] 后端 `check-size` 脚本通过

* [x] 未作答题目不再出现在错题本列表中

* [x] 连续答对次数不因未作答而中断

* [x] eslint提交0错误

