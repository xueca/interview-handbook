# 生产环境变量模板（示例）
# 复制此文件并填入真实密钥：
#   cp backend/.env.production.example backend/.env.production.active
#   vim backend/.env.production.active

NODE_ENV=production
PORT=5000
JWT_SECRET=<使用 openssl rand -hex 32 生成>
DEEPSEEK_API_KEY=<你的 DeepSeek API Key>